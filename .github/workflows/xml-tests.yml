name: XML tests
on: [push, pull_request]
jobs:
  xml:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate XML and patch behavior
        shell: pwsh
        run: ./_tools/test-xml.ps1
